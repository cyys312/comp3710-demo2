# Datasets (large, never committed)
data/
*.zip
*.tar.gz
keras_png_slices_data/

# Training artefacts: weights are too large to commit, but the result figures are kept —
# they are the evidence shown at the demo and the source of the tables in the READMEs
# (well under 1 MB in total).
checkpoints/
runs/
*.pth
*.pt
*.ckpt
*.npy

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/
env/
*.egg-info/

# Editors and OS
.vscode/
.idea/
.DS_Store

# SLURM and local training logs
logs/
slurm-*.out
*.out
*.err

# Scratch directory (safe to delete by hand)
_to_delete/

# Personal progress notes, not part of the submission
STATE.md
commit_and_push.sh
.gitlocks/
