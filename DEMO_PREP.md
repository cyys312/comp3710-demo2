# Demo 准备 — 预期问题与答法

> 评分标准的分配：**代码能跑只占 20 %**，回答问题 40 %，
> 编程规范与 AI 使用说明 20 %，讲述与归属 20 %。
> 也就是说**六成的分在于能不能讲清楚**。代码已经全部就位，这份文档是为那六成准备的。

演示时长：自述约 3 分钟，全程至少 5 分钟，之后是追问。

---

## 0. 三分钟自述的结构

不要按 Part 1→4 平铺直叙，那样时间不够且没有重点。建议：

1. **一句话总览**（15 秒）：四个部分都完成，Part 4 做到 Hard 难度（VAE + UNet + GAN）。
2. **拎出两个最有料的点**（各 60 秒），这是拿高分的地方：
   - **DAWNBench 94.15 % / 95 秒**：讲"先测量、定位瓶颈在 CPU 而非 GPU、再针对性优化"的过程。
   - **UNet 测试集 mean DSC 0.9774**：讲 DSC 该怎么算才对（两处修正）。
3. **工作流**（30 秒）：git 分组提交、本机 MPS 与集群 A100 双环境、slurm 脚本参数实测核对。
4. **AI 使用**（15 秒）：指向 `AI_PROMPTS.md`，强调是"发现并修正 AI 的错误"而非照抄。

---

## Part 1 — DFT

### 必答：为什么最快的方法最快？

四种实现在 N=4096 时的排序（秒）：

| 实现 | N=1024 | N=2048 | N=4096 | 复杂度 |
|---|---|---|---|---|
| NumPy 朴素 DFT | 0.009265 | 0.037120 | 0.146261 | O(N²) |
| PyTorch 朴素 DFT（GPU） | 0.002785 | 0.006693 | 0.010211 | O(N²) |
| NumPy FFT | 0.000006 | 0.000011 | 0.000019 | O(N log N) |
| PyTorch FFT（GPU） | 0.000269 | 0.000236 | 0.000374 | O(N log N) |

**答法**（三层，逐层深入）：

1. **FFT 快是因为算法，不是硬件**。看朴素 DFT 那一行：N 每翻一倍，耗时约 ×4
   （0.0093 → 0.037 → 0.146），正是 O(N²) 的特征；FFT 增长不到 2 倍。
   FFT 靠的是分治——把 N 点 DFT 递归拆成两个 N/2 点 DFT，复用旋转因子的对称性
   （\(W_N^{k+N/2} = -W_N^k\)），把 N² 次复数乘降到 N log N 次。
2. **GPU 让朴素 DFT 快一个数量级，但改不了复杂度**。N=4096 时 GPU 版比 CPU 版快 14 倍，
   因为 N² 次乘加被摊到数千核心上并行；但它仍是 O(N²)，N 再翻几倍照样输给 CPU 上的 FFT。
   **并行度只能改善常数因子，不能改善复杂度。**
3. **GPU 的 FFT 反而不如 NumPy 的**，因为这个规模下每次 kernel 启动的固定开销
   （约 0.25 ms）已经超过计算本身。GPU 要在计算量足够大时才划算——
   注意 Torch FFT 那一行从 N=1024 到 4096 几乎不变，说明测的全是启动开销。

### 可能追问

**Q：你的 naive_dft 用矩阵乘，这还算"朴素"吗？**
算。矩阵形式做的仍是 N² 次复数乘加，复杂度没变，只是把循环下沉到 BLAS。
我专门保留了一个双重 Python 循环版做对照：N=512 时循环版 0.0581 s、
矩阵版 0.0023 s，**快 25 倍但复杂度相同**——这正说明复杂度和常数因子是两件事。

**Q：GPU 版和 NumPy 的结果不完全一样？**
对，相对误差约 1.78e-4。原因是 GPU 版用 complex64（单精度）而 NumPy 用 complex128。
单精度尾数 24 位，累加 N 项后误差约 \(2^{-24}\sqrt{N}\)，量级正好对得上。
NumPy 双精度版与 FFT 的相对误差是 3.04e-13，在双精度的噪声水平上。
**报误差要看相对值**：谱峰幅度约 4N/π ≈ 1300，绝对误差 0.2 其实只有 1e-4 的相对误差。

**Q：为什么方波的频谱只有奇次谐波？**
方波是奇函数且半波对称：\(f(t + T/2) = -f(t)\)。这个对称性使所有偶次谐波的
积分相消，只剩奇次，且幅度按 1/n 衰减。

**Q：Gibbs 现象是什么？增加谐波能消除吗？**
不能。跳变点附近的过冲幅度**恒定约为跳变量的 9 %**，增加谐波只会让过冲变窄、
向不连续点收缩，但高度不变。根本原因是傅里叶级数在不连续点只能逐点收敛而非一致收敛。

---

## Part 2 — Eigenfaces + 随机森林

### 结果与它的参照系

- 随机森林准确率 **0.5776**
- 多数类基线 **0.4130**（George W Bush 一人占测试集 41 %）
- 加 `class_weight='balanced'` 后 **0.7298**，macro-F1 从 0.3022 提到 0.6361

**主动说出基线**——只报 0.5776 会让人以为模型还行，报了基线才看得出提升只有 16 个百分点。

### 必答：为什么成分越多反而越差？

| 主成分数 | 25 | 50 | 100 | 150 |
|---|---|---|---|---|
| 准确率 | 0.6304 | **0.6553** | 0.5807 | 0.5776 |

**答法**：PCA 按方差大小排序，但**方差大 ≠ 对分类有用**。前几十个成分捕捉的是
人脸的主要结构差异；高阶成分捕捉的主要是光照方向与姿态——LFW 是自然场景采集的，
这两个因素的方差很大但与"是谁"无关。

再叠加随机森林的机制：`max_features` 默认是 `sqrt`，150 维时每次分裂只随机抽
约 12 个特征。特征里噪声占比越高，抽到全是噪声的概率越大，单棵树就越弱。
所以维度从 50 增到 150，**信息没增加多少，噪声却稀释了有效特征被抽中的概率**。

### 可能追问

**Q：为什么均值只能用训练集算？**
`compute_pca` 里 `mean = np.mean(X_train, axis=0)`，然后同一个均值同时减到训练集和测试集上。
如果用全体数据算均值，测试集的信息就通过均值泄漏进了模型，评估结果会偏乐观。
同理，SVD 也只在训练集上做。

**Q：混淆矩阵说明了什么？**
几乎所有样本都被判给了 George W Bush（第 4 列）。Ariel Sharon 和 Hugo Chavez
的召回率是 0。这是类别不平衡下的典型退化：模型发现"永远猜多数类"就能拿 41 %，
而随机森林的分裂准则（Gini）本身不惩罚这种偏向。
`class_weight='balanced'` 给少数类的样本加权后，macro-F1 翻了一倍。

**Q：紧致度曲线说明什么？**
前 50 个成分解释 84.59 % 的方差，前 150 个解释 94.65 %。
曲线很快变平说明人脸数据的**内在维度远低于 1850 维的像素空间**——
这正是 PCA 有效的前提。

**Q：SVD 和特征分解是什么关系？**
对去均值后的 X 做 SVD 得 \(X = U S V^T\)，则协方差矩阵
\(X^T X = V S^2 V^T\)。所以 V 的列就是协方差矩阵的特征向量（即主成分），
奇异值的平方 \(S^2\) 正比于特征值。直接对 X 做 SVD 比先算
1850×1850 的协方差矩阵再做特征分解更稳定也更省。

---

## Part 3.1 — LFW CNN

**准确率 0.8509**，对比 Part 2 的 0.5776（或加权后的 0.7298）。

### 必答：CNN 为什么比 PCA + 随机森林好？

三点，按重要性：

1. **保留了二维空间结构**。PCA 把图像拉平成 1850 维向量，像素的相邻关系被丢掉了；
   卷积核在二维上滑动，天然利用了"相邻像素相关"这个先验。
2. **特征提取与分类是联合优化的**。PCA 是无监督的，它按方差找基，
   完全不知道下游任务是分人；CNN 的卷积核是用分类损失的梯度训出来的，
   直接朝"有利于区分这几个人"的方向学。这解释了 Part 2 里"方差大但没用"的成分为什么存在。
3. **非线性与层次性**。PCA 是线性变换，只能做子空间投影；
   CNN 通过 ReLU 堆叠出层次化特征（边缘 → 局部形状 → 面部结构）。

**注意**：也要诚实说出代价——CNN 需要更多数据和算力，且可解释性差；
eigenfaces 可以直接画出来看，卷积核不行。

### 可能追问

**Q：为什么输入要 reshape 成 4D？**
`nn.Conv2d` 期望 (N, C, H, W)。LFW 的 `images` 是 (N, 50, 37)，
需要插入通道维变成 (N, 1, 50, 37)。代码里是 `X[:, None, :, :]`。

**Q：为什么用 sparse categorical cross entropy 而不是 one-hot？**
PyTorch 的 `nn.CrossEntropyLoss` 接受类别索引而非 one-hot，
内部等价于 log_softmax + NLLLoss。少一次 one-hot 展开，省内存也更快。
（**注意与 Task 2 的区别**：那里任务书明确要求 one-hot 输出。）

**Q：Dropout 放在哪里，为什么？**
放在全连接层之间（`self.dropout = nn.Dropout(0.5)`）。
训练集只有 966 张而全连接层参数量大，最容易过拟合的就是这一段。
卷积层参数共享，本身正则性就比较强，通常不需要那么重的 dropout。

---

## Part 3.2 — DAWNBench（4 分，重点讲这个）

### 结果

**94.15 % 准确率 / 95 秒**（A100）。任务书两档都达标：
>90 % 且 <30 分钟 ✓；≥94 % 且 ≤360 秒（V100 基准）✓，快约 3.8 倍。

### 主线故事：先测量，再优化

这是整个 Lab 里最值得讲的一段，因为它体现的是工程方法而不只是调参。

1. **最初的实现**用 torchvision 的 `DataLoader` + PIL 增强管线。
   在 A100 上跑，**227 秒都跑不完一个 epoch**（97 步，即 >2.3 秒/步）。
2. **诊断**：ResNet-18 在 A100 上一步的计算量只有几十毫秒。
   2.3 秒/步意味着 GPU 有 98 % 的时间在等数据。**瓶颈在 CPU 侧，不在模型。**
3. **根因**：Rangpur 的 a100 节点每个只有 8 个 CPU 核（作业申请 4 核），
   PIL 的 `RandomCrop → Flip → ToTensor → Normalize` 是逐张做的 Python 层操作，
   4 个 worker 喂不饱 A100。
4. **对策**：CIFAR-10 的原始像素总共只有 50000×3×32×32 = **153 MB**（uint8），
   相对 40 GB 显存微不足道。一次性搬进显存，随机裁剪与翻转改用张量算子批量完成，
   CPU 彻底退出热路径。
5. **结果**：**1.9 秒/epoch**，提速 100 倍以上。

### 必答：怎么保证 GPU 增强和原来等价？

这个问题一定会被问，因为"自己实现增强"最容易出错。

- **随机裁剪**：先 reflect padding 到 40×40，再对**每个样本独立**取 [0,8] 的
  随机偏移裁回 32×32。与 `T.RandomCrop(32, padding=4, padding_mode="reflect")` 逐样本等价。
  **验证方法**：把偏移固定为 (4,4)，输出必须与原图逐像素相同——我做了这个测试并通过。
  这一条同时验证了高级索引没有写错位（混用切片与高级索引时维度顺序容易搞反）。
- **翻转**：每个样本独立以 0.5 概率翻转，同 `T.RandomHorizontalFlip()`。
- **归一化**：同一组 MEAN/STD，只是先乘回 255——因为跳过了 `ToTensor` 的除以 255。

两条管线都保留了，`--loader {gpu,cpu}` 可切换，现场可以演示对比。

### 可能追问

**Q：为什么首层要改成 3×3 stride=1 并去掉 maxpool？**
ImageNet 版 ResNet-18 首层是 7×7 stride=2 加 3×3 maxpool stride=2，
开头就下采样 4 倍。ImageNet 输入 224×224 承受得起，但 CIFAR 是 32×32——
下采样 4 倍后只剩 8×8，进入 layer1 时空间信息已经所剩无几。
改成 3×3 stride=1 且不 maxpool，layer1 拿到的还是 32×32。

**Q：残差连接解决什么问题？**
不是梯度消失（BatchNorm 已经很大程度缓解了），主要是**退化问题**：
He 等人观察到 56 层网络的**训练**误差反而高于 20 层，说明不是过拟合而是难优化。
残差块学的是 \(F(x) = H(x) - x\)，如果最优映射接近恒等，把 \(F\) 压到 0
比让一堆非线性层拟合出恒等映射容易得多。同时捷径给梯度提供了一条直通路径。

**Q：混合精度为什么既快又不掉精度？GradScaler 做什么？**
快：A100 的 Tensor Core 对 fp16/bf16 的吞吐远高于 fp32，且显存带宽减半。
不掉精度：前向与反向用 fp16，但**权重和优化器状态保持 fp32 主副本**，累加也在 fp32 做。
GradScaler 解决的是 fp16 的动态范围问题——梯度值常常小到 fp16 的次正规数区间而下溢为 0。
它把损失乘一个大的缩放因子再反向，梯度等比放大避开下溢，更新前再除回去；
若检测到 inf/nan 就跳过这一步并调小因子。

**Q：OneCycle 学习率为什么有效？**
先线性升到 max_lr（这里 0.4，很大）再退火到接近 0。
大学习率阶段起到正则作用，帮助跳出尖锐极小值；后期小学习率精细收敛。
配合 label smoothing 0.1，能在 30 个 epoch 这种很短的预算内到 94 %。

**Q：为什么用 channels_last？**
NHWC 布局更契合 Tensor Core 的访存模式，cuDNN 的卷积 kernel 在这个布局下
不需要额外转置，能省一次内存重排。

### 演示时要跑的命令

任务书第 2 条要求**现场在 Rangpur 上跑推理和一个 epoch 的训练**（1 分）：

```bash
sbatch slurm/demo_dawnbench.slurm
```

这个脚本一次做完两件事。注意里面的 `--no-save`：
不加的话，这一个 epoch 的模型会覆盖掉那个 94.15 % 的 checkpoint
（保存条件是 `acc > best_acc` 而 `best_acc` 从 0 开始）。**这个坑值得主动讲**，
它说明你考虑过演示场景下的副作用。

---

## Part 4 Task 1 — VAE

### 结果

| latent_dim | 最佳验证 ELBO | 重建项 | KL 项 |
|---|---|---|---|
| 2 | 4265.93 | 4173.72 | 8.24 |
| 32 | **4183.05** | 4053.01 | 32.46 |

训练了两个模型：2 维用来直接画流形网格，32 维重建更清晰。

### 必答：KL 项的作用？去掉会怎样？

KL 项 \(D_{KL}(q(z|x) \| p(z))\) 把后验拉向标准正态先验。

**去掉它就退化成普通 autoencoder**：编码器可以把每个样本映射到隐空间里
任意孤立的一个点，只要解码器认得就行。后果是隐空间**不连续也无结构**——
在两个训练样本的编码之间插值，解出来的是噪声而非合理的脑图，
也无法通过采样先验来生成新样本。

KL 项强制每个样本的后验都是一个有一定"宽度"的高斯，
这些高斯必须互相重叠着铺满先验，隐空间才被"填满"。

### 必答：怎么知道没有后验坍缩？

后验坍缩（posterior collapse）是 KL → 0：编码器输出的后验退化成先验本身，
隐变量被解码器完全忽略。

**我们的数字**：2 维时 KL = 8.24 nats ≈ 4.1 nats/维；
32 维时 KL = 32.46 nats ≈ 1.0 nats/维。**都明显大于 0**，说明隐变量确实在被使用。
（若坍缩，KL 会趋近于 0 而重建项居高不下。）

### 必答：重参数化技巧是什么，为什么需要？

要优化的目标里有一步"从 \(q(z|x)\) 采样 z"。采样操作本身不可导，
梯度传不回编码器的 μ 和 logvar。

重参数化把随机性移到参数之外：\(z = \mu + \sigma \odot \epsilon\)，
其中 \(\epsilon \sim N(0, I)\) 与参数无关。
这样 z 对 μ、σ 是确定性可导的函数，ε 只是一个外部输入的随机数。

### 可能追问

**Q：流形网格为什么用分位数采样而不是等距？**
先验是标准正态。若在 [−3, 3] 等距取点，网格边缘落在概率密度极低的区域——
那里模型训练时几乎没见过，解出来的图不可信。
按分位数取（`norm.ppf(linspace(0.02, 0.98, n))`）能让网格**均匀覆盖概率质量**。

**Q：流形图说明了什么？**
相邻位置的脑图平滑过渡，中央脑室的蝶形结构沿一个方向连续地由窄变宽、由尖变圆，
没有突变或撕裂。这正是 KL 项作用的直接证据。

**Q：为什么重建有点糊？**
VAE 的重建项是逐像素的 BCE，等价于假设像素条件独立。
在这个假设下，模型对不确定的细节最优的做法是输出**期望值**，也就是平均——
表现出来就是糊。这是 VAE 相对 GAN 的固有短板，
也正是同时做 Task 3 的意义：GAN 用判别器代替逐像素损失，样本更锐利但没有似然可监控。

**Q：latent_dim 怎么选？**
是个取舍：2 维可以直接可视化流形但表达能力有限（ELBO 4265.93）；
32 维重建更好（4183.05）但必须先降维才能看。所以两个都训了。

---

## Part 4 Task 2 — UNet（重点讲这个）

### 结果（测试集 544 张，完全未参与训练与调参）

| 类别 | 组织 | 像素占比 | DSC |
|---|---|---|---|
| 0 | 背景 | 72.30 % | 0.9993 |
| 1 | 脑脊液 CSF | 5.66 % | 0.9651 |
| 2 | 灰质 | 11.39 % | 0.9658 |
| 3 | 白质 | 10.65 % | 0.9795 |
| **mean** | | | **0.9774** |

**四个标签全部 > 0.9，满足要求。**

### 必答：skip connection 为什么对分割是关键？

分割要同时回答两个问题：**"这是什么组织"**（需要大范围上下文）和
**"边界在哪里"**（需要高分辨率细节）。

编码器不断下采样，感受野变大，语义信息越来越强，但空间精度被丢掉了——
到 bottleneck 时特征图只有 16×16，无法精确定位边界。
skip connection 把编码器每一级的高分辨率特征直接拼接到解码器对应级，
**把丢掉的空间细节补回来**。没有它，输出会是语义正确但边界糊成一团的分割。

### 必答：为什么用 CE + Dice 的组合损失？

- **纯 CE 的问题**：背景占 72.30 % 的像素。CE 是逐像素平均的，
  模型只要把所有像素判成背景就能拿到不错的损失值，会系统性偏向多数类。
- **纯 Dice 的问题**：Dice 对小目标的梯度不稳定，尤其当某个类在某张图上
  完全不存在时分母趋零，训练早期容易震荡。
- **各占一半**：CE 提供稳定、逐像素的梯度信号；
  Dice 直接优化评测指标本身，且它是区域重叠度，天然对类别不平衡不敏感。

### 必答：DSC 该怎么算（这里有两处修正，主动讲）

这是最能体现"理解"的一段，务必主动提。

**修正 1：评测必须用硬预测，不能用软 Dice。**
最初的实现直接对 softmax 概率算 Dice，训练损失和评测指标共用一个函数。
这是错的——任务书要求的 DSC 是对 **argmax 之后的类别图**算的。
软 Dice 在模型不自信时偏低、过分自信时偏高，拿它上报会失真。
现在拆成两个函数：`dice_per_class`（软，可导，只给损失用）和
`hard_dice_counts`（硬，只给评测用）。

**修正 2：DSC 是比值，不能按 batch 平均。**
最初是"每个 batch 算一个 DSC 再除以 batch 数"。但
\(\frac{2\sum I_i}{\sum C_i} \neq \frac{1}{n}\sum \frac{2 I_i}{C_i}\)，
而且最后一个不满的 batch 会被赋予同等权重。
现在改为：跨 batch 累加逐类的**交集**与**基数**，全部累完再做一次除法，
得到真正的数据集级 DSC。

### 必答：为什么 c1（CSF）的 DSC 最低？

两个原因叠加：

1. **占比小**：只有 5.66 % 的像素。
2. **形状细长**：CSF 多分布在脑室边缘和脑沟里，**边界像素占其总面积的比例最高**。

Dice 对小目标的边界误差最敏感——错一圈像素，对小结构是很大的相对损失，
对大结构几乎可以忽略。这也正是不能只看 mean DSC 的原因。

### 可能追问

**Q：4 个标签分别是什么组织？你怎么确定的？**
数据集没有给标签字典。我用**各标签区域在原图中的平均灰度**反推：

| 标签 | 平均灰度 |
|---|---|
| c0 | 0.0551 |
| c1 | 0.1407 |
| c2 | 0.3153 |
| c3 | 0.4755 |

灰度严格单调递增，与 T1 加权 MRI 的组织对比度顺序完全一致：
背景最暗 → CSF（T1 上低信号）→ 灰质 → 白质最亮。

**Q：为什么标签 resize 必须用最近邻？**
标签的灰度值只有 {0, 85, 170, 255} 四档，它们是**类别编号**不是连续量。
双线性插值会在边界处产生 42、127 这样的中间值，
映射回类别时就会凭空造出不存在的类别或错误的类别归属。
最近邻不产生新值，所以标签必须用它；原图是连续量，用双线性更平滑。

**Q：为什么不重新划分数据集？**
数据集自带的 train/validate/test 是**按病例（case）分开**的。
同一个病例的相邻切片高度相关——`case_441_slice_10` 和 `slice_11` 几乎一样。
如果随机重新切分，同一病例的切片会同时落进训练集和测试集，
造成严重的信息泄漏，测试分数会虚高。

**Q：one-hot 输出是什么意思？**
任务书明确要求 categorical / one-hot 输出。
网络输出 4 通道的 logits（每个像素一个 4 维向量），
标签用 `F.one_hot` 转成 (B, 4, H, W)，与 Dice 损失逐通道对齐。
不用 one-hot 会被扣分。

**Q：mean DSC 0.9774 是不是过拟合？**
不是。这个数字来自 **544 张测试集切片**，该划分从头到尾没参与训练与调参；
checkpoint 是按**验证集**平均 DSC 选的（第 22 个 epoch），测试集只在最后评估一次。
验证集的对应数字是 0.9770，两者非常接近，说明泛化良好。

### 演示时要跑的命令

任务书明确要求**现场对测试集跑推理**：

```bash
sbatch --export=ALL,ONLY=unet slurm/predict.slurm
```

配合 `outputs/segmentation_examples.png`（原图 / 真值 / 预测三列）讲解。

---

## Part 4 Task 3 — GAN

### 必答：怎么证明没有 mode collapse？

**不要说"你看图挺多样的"**——那是主观判断。用数字：

> 多样性 = 样本两两之间的平均 L2 距离，与**真实数据的同一指标**做比值。

崩塌的生成器把不同的 z 映射到少数几个模式，样本彼此近乎相同，这个数会远低于真实数据。

| epoch | 6 | 15 | 43 | 80（最终） |
|---|---|---|---|---|
| 多样性 / 真实数据 | 24 % | 65 % | 90 % | **84 %** |

**独立复核**：`predict.py` 用**测试集**（544 张，完全未参与训练）作基准重算，
得 **88 %**（26.70 / 30.31）。训练时那个 84 % 用的是当轮最后一个训练 batch 作基准。
两个数一致且测试集上略好——说明这个指标是稳的，不是挑出来的。

### 必答：为什么曲线是 V 形？第 6 轮的 24 % 不是崩塌吗？

**不是**，这个区别值得讲清楚：

训练初期，生成器最省力的策略是输出所有脑图的"平均"——单张看起来像个模糊的脑，
能骗过还很弱的判别器，但彼此几乎一样，所以多样性先跌到 24 %。
等判别器学会识破这种平均图之后，生成器被迫去覆盖真实分布的各个区域，
多样性才开始爬升，最终到 90 %。

**低点不是 mode collapse，区别在于能否自行恢复**：
真正的崩塌会一直停在低位甚至继续恶化。

同期损失也印证：判别器损失稳定在 1.43–1.48（接近均衡值 ln 4 ≈ 1.386），
生成器损失从 1.20 降到 0.85，没有任何一方"赢死"另一方。

### 必答：你做了哪些事来防止崩塌？

四项，每项针对一个具体失效模式：

| 措施 | 针对的失效模式 |
|---|---|
| **R1 梯度惩罚** | 判别器过强 → 生成器梯度消失。惩罚 D 在**真实样本处**梯度的平方范数，约束成局部 Lipschitz。惰性正则每 16 步做一次（要算二阶导，较贵） |
| **生成器 EMA** | 采样时权重震荡。用指数滑动平均后的权重采样 |
| **TTUR** | D/G 强弱失衡。D 用 3e-4、G 用 2e-4，让 D 保持"稍强但不过强" |
| **标签平滑 0.9** | D 进入饱和区后梯度≈0。不让它把置信度推到 1.0 |

另外两处：判别器用 **InstanceNorm 而非 BatchNorm**（后者让 batch 内样本
通过统计量互相耦合，是已知的不稳定来源）；生成器用**非饱和损失**
（最大化 log D(G(z)) 而非最小化 log(1−D(G(z)))，后者在早期 D 很强时梯度几乎为 0，
这是原始 GAN 论文就指出的问题）。

### 可能追问

**Q：为什么数据要归一化到 [−1, 1] 而不是 [0, 1]？**
生成器最后一层是 Tanh，输出范围就是 [−1, 1]。
真假数据的取值范围必须一致，否则判别器只要看数值范围就能分辨，训练立刻退化。

**Q：损失值能说明生成质量吗？**
**不能**，这是 GAN 最反直觉也最难调的地方。
D 和 G 是对抗的，损失下降可能只是意味着对手变弱了。
VAE 有 ELBO 这个明确的似然下界可以监控（下降就是在进步），GAN 没有。
所以才需要多样性指标这类**外部**评价，以及肉眼看样本。

**Q：为什么用固定噪声保存样本？**
`samples_epoch{NNN}.png` 全部用同一组固定的 z 生成。
这样不同 epoch 之间可比——看到的变化全部来自模型本身，而不是换了随机数。

**Q：插值图说明了什么？**
`interpolation.png` 是 8 行 × 10 步的隐空间线性插值。每一行都是**连续形变**——
脑室从宽变窄、从蝶形收成裂缝，颅骨轮廓同步微调，全程没有突跳。
崩塌的生成器会在途中从一个模式硬切到另一个。这是独立于多样性指标的第二条证据。

### 主动说出局限（比被问出来好）

- **样本左右对称性偏强**。真实脑切片是近似对称而非严格对称；
  数据经过配准且 128×128 下细微不对称信息有限，生成器学到了"对称"这个捷径。
- **只覆盖中部轴位层面** —— 这是忠实反映训练数据，不是缺陷。
- **没做 FID**。多样性指标能证明"没崩塌"，不能完整衡量"有多真实"。

---

## 工作流与工程实践（20 % 的分在这里）

### Git

- 11+ 个 commit，**按 Part 分组**，每条 message 说明**改动的理由**而非只说改了什么。
- 远端原本有一个 initial commit，本地历史是从 Lab 1 仓库带过来的独立根。
  处理方式：`git reset --soft origin/main` 把分支指针移到远端，工作树不动，
  再重新分组提交。这样历史线性、**不需要 force push**、远端的 commit 也没丢。
  （对比：AI 最初给的脚本写的是 `push --force-with-lease`，那会覆盖掉远端那个 commit。）
- `.gitignore` 排除数据集与 checkpoint，但**保留 `outputs/` 下的结果图**——
  它们是演示证据和 README 结果表格的出处，总共不到 1 MB。

### 双环境

代码在本机（Apple MPS）和集群（CUDA）上都能跑，靠 `pick_device()` 与 `auto_workers()`
自动适配。实测发现的一个反直觉结论值得讲：

**在 Mac 上 `num_workers=0` 比 `num_workers=6` 快 29 倍**
（9 ms/batch vs 262 ms/batch）。因为单张 OASIS 切片解码只要约 1 ms，
多进程的 IPC 序列化开销远大于收益。所以 `auto_workers()` 在 CUDA 上用 4、
在 MPS/CPU 上用 0——**默认值不能照抄，要按环境实测**。

### 集群配置（实测核对，不是照抄模板）

用 `sinfo` / `scontrol show node` / `sacctmgr` 逐条核对，修正了三处：

| 常见写法 | 实际情况 | 后果 |
|---|---|---|
| `--mem=32G` | 所有 a100 节点 `RealMemory=1` | 作业**无限期 PENDING** |
| `--partition=comp3710` | `AllowAccounts=comp3710`，账号关联未开通 | `PENDING (PartitionConfig)` |
| `module load cuda` | conda 环境是 torch 2.13.0+**cu130**，自带运行时 | 版本冲突风险 |

另外：**登录节点不跑计算任务**，需要下载数据就提交 `--partition=cpu` 的作业。

---

## AI 使用（务必主动讲，这是 20 % 的一部分）

评分标准写明「把题目抄给 AI、把答案抄回来」**不算合格用法**，
要求展示"精炼提示、尝试不同方法、评估并改进 AI 生成的结果"。

**答法**：指向 `AI_PROMPTS.md`，重点讲**发现并修正了 AI 的哪些错误**：

1. Part 1 漏掉了任务书要求的 PyTorch / GPU 实现——重读任务书才发现
2. DSC 用软 Dice 当评测指标（应该用硬预测）
3. DSC 按 batch 平均（比值不能这样平均）
4. slurm 的三个参数是按模板猜的，实测后全错
5. 多进程 DataLoader 在 Mac 上是负优化（慢 29 倍）
6. 标签解码为每张图分配 H×W×4 的临时数组（改成 256 项查找表）

以及**自己补做的**：Part 2 的基线与两组消融、
Part 3.2 的 GPU 数据管线与等价性验证、Task 3 的多样性指标。

**可能被要求提供 prompt 历史** —— `AI_PROMPTS.md` 就是这份材料。

---

## 演示前的检查清单

- [ ] UQ VPN 已连（否则 ssh 不通）
- [ ] `ssh rangpur` 能登上
- [ ] 集群上代码是最新的（`git pull` 或 rsync）
- [ ] checkpoint 都在：`part3_cnn/dawnbench/checkpoints/`、
      `recognition/{unet,vae,gan}_oasis/checkpoints/`
- [ ] 演示命令预跑一遍，确认能出结果：
      - `sbatch slurm/demo_dawnbench.slurm`（Part 3.2，明确的 1 分）
      - `sbatch --export=ALL,ONLY=unet slurm/predict.slurm`（Task 2，任务书要求）
- [ ] GitHub 仓库能当场登录展示（任务书说 demonstrator 可能要求证明是你本人的账号）
- [ ] 学生证带上（用于身份确认）
- [ ] `AI_PROMPTS.md` 能当场打开
