# COMP3710 Lab 2 — runtime environment (Python 3.10+)
# For a GPU build of torch, install it separately per the CUDA instructions at https://pytorch.org
torch>=2.0
torchvision>=0.15
numpy>=1.24
matplotlib>=3.7
scikit-learn>=1.3
scipy>=1.10
Pillow>=10.0
tqdm>=4.65
umap-learn>=0.5      # Task 1 VAE manifold visualisation (optional; a sampling grid works too)
